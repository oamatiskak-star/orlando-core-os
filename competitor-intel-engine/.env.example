# competitor-intel-engine — vereiste env vars
# Op de Mac Mini levert PM2 deze; lokaal kan een .env of ../.env.gh-secrets gebruikt worden.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Optioneel — zonder key slaat de engine alleen de ruwe (echte) extractie op, geen AI-verrijking.
ANTHROPIC_API_KEY=
# Optioneel
PORT=3007
COMPETITOR_CRON_HOUR=4
DOTENV_PATH=
